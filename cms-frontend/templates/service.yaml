apiVersion: v1
kind: Service
metadata:
  name: {{ include "cms-frontend.fullname" . }}
  labels:
    {{- include "cms-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cms-frontend.selectorLabels" . | nindent 4 }}
